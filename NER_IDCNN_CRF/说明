
num_chars = vocab_size  词汇表大小
data/vec.txt：wikipedia中文语料上预训练好的中文的字向量(vec.txt)

id_to_tag.txt和id_to_tag.txt是正反向字典
vec.txt是词向量，每个都是100维的，即时是一个逗号。
与聊天机器人不同的地方，之前每个字是转化为数值，这里是转化为向量
main里面放的是测试的结果，main2是训练的过程
只要不是函数的，就会从上往下去运行，运行到define就会跳过

该模型是一个带CRF层的双向LSTM神经网络将汉字序列投影到稠密向量序列中，并结合额外的特征作为递归层的输入，
本文采用一个表示字边界特征的热向量来说明递归层是一个双向的LSTM层，前向和后向向量的输出被浓缩并投影到
每个标签的得分上使用CRF层来克服标签偏差问题。

我们的模型类似于基于字符的LSTM-CRF中最新的中文命名实体识别模型，该模型具有中文命名实体识别的基本特征。